# HeyMail Security Policy

Security findings should never be demonstrated by committing credentials, private keys, access tokens or real message contents to this repository.

If a secret is exposed, treat it as compromised and rotate it rather than relying only on deletion from the latest Git commit.

The project will progressively document threat models, security tests and remediation decisions under `docs/security/` and `docs/journal/`.
