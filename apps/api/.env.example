APP_ENV=dev
APP_DEBUG=1

# Paths only. Secret contents must never be stored here.
APP_SECRET_FILE=/run/secrets/app_secret

DB_HOST=database
DB_PORT=5432
DB_NAME=heymail
DB_USER=heymail_app
DB_PASSWORD_FILE=/run/secrets/postgres_app_password
